async function testfunc(msg, name)
{
aaa = msg + ' test 1 ' + name;
var resp = await fetch('https://raw.githubusercontent.com/Ssid113/nightbot/master/botik/all_commands');
var answer = await resp.text();
return aaa;
}


test = await testfunc(msg, name);
test;
